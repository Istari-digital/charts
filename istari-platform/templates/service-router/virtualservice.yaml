{{- $serviceValues := .Values.serviceRouter -}}
{{- if and $serviceValues.enabled $serviceValues.virtualService.enabled }}
{{- if not $serviceValues.virtualService.hosts -}}
{{- fail "serviceRouter.virtualService.hosts must contain at least one entry when serviceRouter.virtualService.enabled is true" -}}
{{- end }}
{{- $svcName := include "serviceRouter.fullname" . -}}
apiVersion: networking.istio.io/v1
kind: VirtualService
metadata:
  name: {{ $svcName | quote }}
  labels:
    {{- include "serviceRouter.labels" . | nindent 4 }}
    {{- with $serviceValues.virtualService.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- with $serviceValues.virtualService.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  hosts:
    {{- toYaml $serviceValues.virtualService.hosts | nindent 4 }}
  {{- with $serviceValues.virtualService.gateways }}
  gateways:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  http:
  - route:
    - destination:
        host: {{ $svcName | quote }}
        port:
          number: 80
{{- end }}
