{{- $serviceValues := .Values.serviceRouter -}}
{{- if $serviceValues.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "serviceRouter.fullname" . | quote }}
  labels:
    {{- include "serviceRouter.labels" . | nindent 4 }}
  {{- with $serviceValues.serviceAnnotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ $serviceValues.serviceType | quote }}
  ports:
  - name: "http"
    port: 80
    targetPort: "http"
    protocol: "TCP"
  selector:
    {{- include "serviceRouter.selectorLabels" . | nindent 4 }}
{{- end }}
