{{- $serviceValues := .Values.apiGateway -}}
{{- if $serviceValues.enabled }}
{{- if $serviceValues.autoscaling.enabled }}
{{- if not (or $serviceValues.autoscaling.cpuUtilization $serviceValues.autoscaling.memoryUtilization) }}
{{- fail "apiGateway.autoscaling is enabled but cpuUtilization and memoryUtilization are both null — set at least one so the HPA has a metric to scale on" }}
{{- end }}
{{- /* Utilization is a percentage of the pod's request; without the matching request the HPA silently never scales. */}}
{{- $requests := dig "resources" "requests" (dict) $serviceValues }}
{{- if and $serviceValues.autoscaling.cpuUtilization (not (dig "cpu" "" $requests)) }}
{{- fail "apiGateway.autoscaling scales on CPU utilization but apiGateway.resources.requests.cpu is not set — the HPA cannot compute a percentage without a request. Set apiGateway.resources.requests.cpu (e.g. 100m), or set apiGateway.autoscaling.cpuUtilization to null" }}
{{- end }}
{{- if and $serviceValues.autoscaling.memoryUtilization (not (dig "memory" "" $requests)) }}
{{- fail "apiGateway.autoscaling scales on memory utilization but apiGateway.resources.requests.memory is not set — the HPA cannot compute a percentage without a request. Set apiGateway.resources.requests.memory (e.g. 128Mi), or set apiGateway.autoscaling.memoryUtilization to null" }}
{{- end }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "api-gateway.fullname" . | quote }}
  labels:
    {{- include "api-gateway.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "api-gateway.fullname" . | quote }}
  minReplicas: {{ $serviceValues.autoscaling.minReplicas }}
  maxReplicas: {{ $serviceValues.autoscaling.maxReplicas }}
  metrics:
  {{- if $serviceValues.autoscaling.cpuUtilization }}
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: {{ $serviceValues.autoscaling.cpuUtilization }}
  {{- end }}
  {{- if $serviceValues.autoscaling.memoryUtilization }}
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: {{ $serviceValues.autoscaling.memoryUtilization }}
  {{- end }}
{{- end }}
{{- end }}
