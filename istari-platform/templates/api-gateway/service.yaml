{{- $serviceValues := .Values.apiGateway -}}
{{- if $serviceValues.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "api-gateway.fullname" . | quote }}
  labels:
    {{- include "api-gateway.labels" . | nindent 4 }}
  {{- with $serviceValues.serviceAnnotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ $serviceValues.serviceType | quote }}
  ports:
  - name: "http"
    port: 80
    targetPort: "http"
    protocol: "TCP"
  selector:
    {{- include "api-gateway.selectorLabels" . | nindent 4 }}
{{- end }}
