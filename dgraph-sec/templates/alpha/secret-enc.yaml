{{ if and .Values.alpha.encryption.enabled (not .Values.alpha.encryption.existingSecret) .Values.alpha.encryption.file }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ template "dgraph-sec.alpha.fullname" . }}-encryption-secret
  namespace: {{ include "dgraph-sec.namespace" . }}
  labels:
    {{- include "dgraph-sec.labels" (dict "ctx" . "component" .Values.alpha.name) | nindent 4 }}
type: Opaque
data:
  {{- with .Values.alpha.encryption.file }}
    {{- toYaml . | trimSuffix "\n" | nindent 2 }}
  {{- end }}
{{ end }}
