{{- if and (or .Values.backups.full.enabled .Values.backups.incremental.enabled) .Values.backups.nfs.enabled -}}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ template "dgraph-sec.backups.fullname" . }}-claim
  namespace: {{ include "dgraph-sec.namespace" . }}
  labels:
    {{- include "dgraph-sec.labels" (dict "ctx" . "component" .Values.backups.name) | nindent 4 }}
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ""
  volumeName: {{ template "dgraph-sec.backups.fullname" . }}-fileserver
  resources:
    requests:
      storage: {{ .Values.backups.nfs.storage }}
{{- end }}
