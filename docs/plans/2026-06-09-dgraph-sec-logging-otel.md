# Dgraph-sec Configurable Log Level + Logging/OTEL Docs Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add first-class, per-role logging values to the `dgraph-sec` chart, wire them to the Dgraph command line, document standard logging and OTEL tracing in the README, and wire the log level through helm-stack's `dgraph-sec.tf` (PR #742).

**Architecture:** Two `_helpers.tpl` helpers (`dgraph-sec.verbosity` maps a named level to a glog `-v` integer; `dgraph-sec.logFlags` renders the per-role flag fragment) keep the three command sites DRY. Five new per-role values (`logLevel`, `vmodule`, `logtostderr`, `alsologtostderr`, `logDir`) sit flat under `alpha:`/`zero:` beside the existing `extraFlags`, which stays as the fallthrough escape hatch. `helm-docs` regenerates the README values table; two hand-written README sections document logging and tracing. The helm-stack side adds two per-role Terraform variables defaulting to `normal`.

**Tech Stack:** Helm 3, Go templates, glog, helm-docs v1.14.2, pre-commit (helmlint + helm-docs), Terraform.

**Design doc:** `docs/designs/2026-06-09-dgraph-sec-logging-otel-design.md`

**Repos / branches:**
- Chart: `istari-digital/charts`, branch `DGR-237-dgraph-sec-logging-otel` (already created).
- Terraform: `istari-digital/helm-stack`, branch `add-dgraph-sec-chart` (PR #742, already open).

**Prefix every `git`/`gh` command** with `gh auth switch --user mwelles-istari >/dev/null 2>&1 &&` (both repos are under istari-digital).

**Render-assert helper command** (run from `charts/` repo root):
```bash
helm template t ./dgraph-sec --show-only templates/alpha/statefulset.yaml [--set ...] | grep -n 'dgraph-sec alpha'
helm template t ./dgraph-sec --show-only templates/zero/statefulset.yaml  [--set ...] | grep -n 'exec dgraph-sec zero'
```

---

## File Structure

| File | Responsibility | Change |
|------|----------------|--------|
| `dgraph-sec/templates/_helpers.tpl` | Template helpers | Add `dgraph-sec.verbosity` + `dgraph-sec.logFlags` |
| `dgraph-sec/values.yaml` | Default values + helm-docs annotations | Add 5 logging keys under `alpha:` and under `zero:` |
| `dgraph-sec/templates/alpha/statefulset.yaml` | Alpha command | Insert `logFlags` before `extraFlags` (1 site) |
| `dgraph-sec/templates/zero/statefulset.yaml` | Zero command | Insert `logFlags` before `extraFlags` (2 sites) |
| `dgraph-sec/README.md.gotmpl` | README source | Add `## Logging` and `## Tracing (OpenTelemetry)` |
| `dgraph-sec/README.md` | Generated README | Regenerated by helm-docs |
| `istari-k8s-core/variables.tf` (helm-stack) | TF variables | Add 2 per-role log-level variables |
| `istari-k8s-core/dgraph-sec.tf` (helm-stack) | helm_release values | Wire `logLevel` into `alpha`/`zero` blocks |

---

## Task 1: Add the verbosity + logFlags helpers

**Files:**
- Modify: `dgraph-sec/templates/_helpers.tpl` (after the `dgraph-sec.raftIndexFlag` block, ~line 359)

- [ ] **Step 1: Add both helpers**

Insert immediately after the `dgraph-sec.raftIndexFlag` define block:

```gotemplate
{{- /* Map a named log level to its glog -v integer; pass any other value
       (e.g. a raw integer) through unchanged. Names are lowercase. */}}
{{- define "dgraph-sec.verbosity" -}}
  {{- $m := dict "normal" "0" "verbose" "1" "debug" "2" "trace" "3" -}}
  {{- $k := toString . -}}
  {{- index $m $k | default $k -}}
{{- end -}}

{{- /* Render the glog flag fragment for a role. `.` is a role value map
       (.Values.alpha or .Values.zero). logLevel and logtostderr always emit;
       vmodule/alsologtostderr/logDir emit only when set. */}}
{{- define "dgraph-sec.logFlags" -}}
-v={{ include "dgraph-sec.verbosity" .logLevel }} --logtostderr={{ .logtostderr }}{{ if .vmodule }} --vmodule={{ .vmodule }}{{ end }}{{ if .alsologtostderr }} --alsologtostderr{{ end }}{{ if .logDir }} --log_dir={{ .logDir }}{{ end }}
{{- end -}}
```

- [ ] **Step 2: Commit** (helper is exercised by Task 3/4 render-asserts)

```bash
gh auth switch --user mwelles-istari >/dev/null 2>&1 && \
git add dgraph-sec/templates/_helpers.tpl && \
git commit -m "feat(dgraph-sec): add glog verbosity + logFlags template helpers"
```

---

## Task 2: Add per-role logging values

**Files:**
- Modify: `dgraph-sec/values.yaml` (zero block, before `extraFlags` ~line 164; alpha block, before `extraFlags` ~line 421)

- [ ] **Step 1: Insert the logging block in the ZERO section**

Find this anchor (under the `zero:` block):

```yaml
  ## Extra flags for command line flags in command
  # -- Extra command-line flags appended to dgraph-sec zero.
  extraFlags: ""
```

Replace it with:

```yaml
  ## --- Logging (glog) -------------------------------------------------------
  ## Verbosity (glog -v). INFO/WARNING/ERROR are ALWAYS written to stderr; this
  ## only adds finer V-logs on top. Named level OR a raw -v integer:
  ##   normal->0 (prod default)   verbose->1   debug->2   trace->3
  # -- Zero log verbosity: named level (normal|verbose|debug|trace) or a raw glog -v integer.
  logLevel: normal

  ## Per-module verbosity (glog --vmodule), e.g. "server=3,raft=2". Empty = none.
  # -- Zero per-module glog verbosity (--vmodule); empty disables.
  vmodule: ""

  ## Log to the container's stderr (glog --logtostderr). Keep true in Kubernetes
  ## so logs reach `kubectl logs` / the node collector. Setting false routes logs
  ## to files under logDir instead — only safe with a writable, mounted logDir.
  # -- Send zero logs to stderr (--logtostderr). Keep true for Kubernetes log capture.
  logtostderr: true

  ## Also write logs to files under logDir IN ADDITION to stderr
  ## (glog --alsologtostderr). Requires a writable logDir.
  # -- Also write zero logs to files under logDir (--alsologtostderr).
  alsologtostderr: false

  ## Directory for glog file output (glog --log_dir); used when logtostderr=false
  ## or alsologtostderr=true. Must be a writable mount. Empty = glog default.
  # -- Directory for zero glog file output (--log_dir); needs a writable mount.
  logDir: ""

  ## Extra flags for command line flags in command. Fallthrough for any glog
  ## option not exposed above; a -v here overrides logLevel (Dgraph takes the
  ## last flag).
  # -- Extra command-line flags appended to dgraph-sec zero (logging fallthrough).
  extraFlags: ""
```

- [ ] **Step 2: Insert the logging block in the ALPHA section**

Find this anchor (under the `alpha:` block):

```yaml
  ## Extra flags for command line flags in command
  # -- Extra command-line flags appended to dgraph-sec alpha.
  extraFlags: ""
```

Replace it with the same block as Step 1 but with every `zero`/`Zero` replaced by `alpha`/`Alpha`:

```yaml
  ## --- Logging (glog) -------------------------------------------------------
  ## Verbosity (glog -v). INFO/WARNING/ERROR are ALWAYS written to stderr; this
  ## only adds finer V-logs on top. Named level OR a raw -v integer:
  ##   normal->0 (prod default)   verbose->1   debug->2   trace->3
  # -- Alpha log verbosity: named level (normal|verbose|debug|trace) or a raw glog -v integer.
  logLevel: normal

  ## Per-module verbosity (glog --vmodule), e.g. "server=3,raft=2". Empty = none.
  # -- Alpha per-module glog verbosity (--vmodule); empty disables.
  vmodule: ""

  ## Log to the container's stderr (glog --logtostderr). Keep true in Kubernetes
  ## so logs reach `kubectl logs` / the node collector. Setting false routes logs
  ## to files under logDir instead — only safe with a writable, mounted logDir.
  # -- Send alpha logs to stderr (--logtostderr). Keep true for Kubernetes log capture.
  logtostderr: true

  ## Also write logs to files under logDir IN ADDITION to stderr
  ## (glog --alsologtostderr). Requires a writable logDir.
  # -- Also write alpha logs to files under logDir (--alsologtostderr).
  alsologtostderr: false

  ## Directory for glog file output (glog --log_dir); used when logtostderr=false
  ## or alsologtostderr=true. Must be a writable mount. Empty = glog default.
  # -- Directory for alpha glog file output (--log_dir); needs a writable mount.
  logDir: ""

  ## Extra flags for command line flags in command. Fallthrough for any glog
  ## option not exposed above; a -v here overrides logLevel (Dgraph takes the
  ## last flag).
  # -- Extra command-line flags appended to dgraph-sec alpha (logging fallthrough).
  extraFlags: ""
```

- [ ] **Step 3: Confirm the chart still parses**

Run: `helm template t ./dgraph-sec >/dev/null && echo OK`
Expected: `OK` (no template/parse error).

- [ ] **Step 4: Commit**

```bash
gh auth switch --user mwelles-istari >/dev/null 2>&1 && \
git add dgraph-sec/values.yaml && \
git commit -m "feat(dgraph-sec): add per-role logging values (logLevel, vmodule, logtostderr, alsologtostderr, logDir)"
```

---

## Task 3: Wire logFlags into the Alpha command

**Files:**
- Modify: `dgraph-sec/templates/alpha/statefulset.yaml:202`

- [ ] **Step 1: Insert the helper before `extraFlags`**

Find (one occurrence):

```
--zero {{ template "dgraph-sec.multiZeros" . }} {{ .Values.alpha.extraFlags }}
```

Replace with:

```
--zero {{ template "dgraph-sec.multiZeros" . }} {{ template "dgraph-sec.logFlags" .Values.alpha }} {{ .Values.alpha.extraFlags }}
```

- [ ] **Step 2: Render-assert defaults**

Run: `helm template t ./dgraph-sec --show-only templates/alpha/statefulset.yaml | grep 'dgraph-sec alpha'`
Expected: command contains `-v=0 --logtostderr=true` and NO `--vmodule`, `--alsologtostderr`, or `--log_dir`.

- [ ] **Step 3: Render-assert a named level**

Run: `helm template t ./dgraph-sec --show-only templates/alpha/statefulset.yaml --set alpha.logLevel=debug | grep -o '\-v=[0-9]*'`
Expected: `-v=2`

- [ ] **Step 4: Render-assert an integer passthrough + the conditional flags**

Run:
```bash
helm template t ./dgraph-sec --show-only templates/alpha/statefulset.yaml \
  --set alpha.logLevel=5 --set alpha.vmodule='server=3' \
  --set alpha.alsologtostderr=true --set alpha.logDir=/var/log/dgraph \
  | grep -o '\-v=5 --logtostderr=true --vmodule=server=3 --alsologtostderr --log_dir=/var/log/dgraph'
```
Expected: the full flag string echoes back (all five render, in order, before `extraFlags`).

- [ ] **Step 5: Commit**

```bash
gh auth switch --user mwelles-istari >/dev/null 2>&1 && \
git add dgraph-sec/templates/alpha/statefulset.yaml && \
git commit -m "feat(dgraph-sec): wire logging flags into the alpha command"
```

---

## Task 4: Wire logFlags into both Zero commands

**Files:**
- Modify: `dgraph-sec/templates/zero/statefulset.yaml:136,138`

- [ ] **Step 1: Insert the helper before `extraFlags` in BOTH zero commands**

Both occurrences share this substring — replace ALL occurrences:

Find:

```
--replicas {{ .Values.zero.shardReplicaCount }} {{ .Values.zero.extraFlags }}
```

Replace with:

```
--replicas {{ .Values.zero.shardReplicaCount }} {{ template "dgraph-sec.logFlags" .Values.zero }} {{ .Values.zero.extraFlags }}
```

(Use a replace-all edit; the string appears in the ordinal-0 command at `:136` and the `--peer` command at `:138`.)

- [ ] **Step 2: Render-assert defaults on both commands**

Run: `helm template t ./dgraph-sec --show-only templates/zero/statefulset.yaml | grep -c 'exec dgraph-sec zero.* -v=0 --logtostderr=true'`
Expected: `2` (both command branches carry the flags).

- [ ] **Step 3: Render-assert a named level**

Run: `helm template t ./dgraph-sec --show-only templates/zero/statefulset.yaml --set zero.logLevel=trace | grep -c '\-v=3'`
Expected: `2`

- [ ] **Step 4: Commit**

```bash
gh auth switch --user mwelles-istari >/dev/null 2>&1 && \
git add dgraph-sec/templates/zero/statefulset.yaml && \
git commit -m "feat(dgraph-sec): wire logging flags into both zero commands"
```

---

## Task 5: Document logging and tracing in the README

**Files:**
- Modify: `dgraph-sec/README.md.gotmpl` (insert before `{{ template "chart.sourcesSection" . }}`, ~line 99)
- Regenerate: `dgraph-sec/README.md`

- [ ] **Step 1: Add the two sections**

Find:

```gotemplate
{{ template "chart.sourcesSection" . }}
```

Insert BEFORE it:

```markdown
## Logging

Dgraph uses glog. Each role writes `INFO` / `WARNING` / `ERROR` to stderr, where
`kubectl logs` and the node log collector capture them. Those severities are always
on; verbosity only adds finer detail on top, so a log level can climb above the
baseline but never hide a severity.

Set verbosity per role with `alpha.logLevel` / `zero.logLevel` — a named level or a
raw glog `-v` integer:

| Level | `-v` |
|-------|------|
| `normal` (default) | 0 |
| `verbose` | 1 |
| `debug` | 2 |
| `trace` | 3 |

Target one subsystem with `vmodule` (e.g. `"server=3,raft=2"`). Keep
`logtostderr: true` on Kubernetes; setting it `false` without a writable `logDir`
loses logs when a container restarts. For any glog flag the chart does not expose
(`--stderrthreshold`, `--log_backtrace_at`), append it to `extraFlags` — a `-v` set
there overrides `logLevel`.

## Tracing (OpenTelemetry)

Set `tracing.enabled` to export traces from Alpha and Zero. The chart builds Dgraph's
`--trace` superflag from the `tracing` block and sends OTLP/HTTP to the configured
collector:

| Value | Purpose |
|-------|---------|
| `tracing.endpoint` | OTLP/HTTP collector — default the Datadog agent on port 4318 |
| `tracing.ratio` | Sample fraction — default `0.01` |
| `tracing.alpha.service` / `tracing.zero.service` | Per-role service name (`dgraph-sec.alpha` / `dgraph-sec.zero`) |

In helm-stack `tracing.enabled` follows the Datadog operator, so traces flow wherever
the agent runs.

```

- [ ] **Step 2: Regenerate the README and lint**

Run: `pre-commit run helm-docs --all-files && pre-commit run helmlint --all-files`
Expected: both `Passed`. `helm-docs` rewrites `dgraph-sec/README.md`; the new logging values appear as rows in the values table, and the two prose sections render.

- [ ] **Step 3: Verify the values table picked up the new keys**

Run: `grep -E 'alpha.logLevel|zero.logLevel|alpha.vmodule|alpha.logtostderr' dgraph-sec/README.md`
Expected: rows for the new keys exist.

- [ ] **Step 4: Commit**

```bash
gh auth switch --user mwelles-istari >/dev/null 2>&1 && \
git add dgraph-sec/README.md.gotmpl dgraph-sec/README.md && \
git commit -m "docs(dgraph-sec): document standard logging and OTEL tracing"
```

---

## Task 6: Full chart validation

**Files:** none (verification only)

- [ ] **Step 1: Run the full pre-commit suite**

Run: `pre-commit run --all-files`
Expected: all hooks `Passed` or `Skipped`; no file left modified by a hook (re-run if helm-docs/yamlfmt rewrote anything, then re-stage and amend the relevant commit).

- [ ] **Step 2: Lint the chart directly**

Run: `cd dgraph-sec && helm lint . && cd ..`
Expected: `1 chart(s) linted, 0 chart(s) failed`.

- [ ] **Step 3: Confirm no stray default-render changes**

Run: `helm template t ./dgraph-sec | grep -E '\-v=|--logtostderr|--trace' | head`
Expected: alpha and zero commands show `-v=0 --logtostderr=true`; `--trace` absent at defaults (tracing off).

---

## Task 7: helm-stack — wire the log level through dgraph-sec.tf (PR #742)

**Repo:** `istari-digital/helm-stack`, branch `add-dgraph-sec-chart`. Obtain a working copy (worktree or checkout) of that branch before editing. Do NOT push to this open PR branch without confirming with the user first.

**Files:**
- Modify: `istari-k8s-core/variables.tf` (near the other `dgraph_sec_*` variables)
- Modify: `istari-k8s-core/dgraph-sec.tf` (`alpha`/`zero` blocks inside the `helm_release` `yamlencode` values)

- [ ] **Step 1: Add the two variables**

Add near the existing `dgraph_sec_*` variables in `variables.tf`:

```hcl
variable "dgraph_sec_alpha_log_level" {
  description = "Dgraph-sec alpha log verbosity: a named level (normal|verbose|debug|trace) or a raw glog -v integer."
  type        = string
  default     = "normal"
}

variable "dgraph_sec_zero_log_level" {
  description = "Dgraph-sec zero log verbosity: a named level (normal|verbose|debug|trace) or a raw glog -v integer."
  type        = string
  default     = "normal"
}
```

- [ ] **Step 2: Wire `logLevel` into the `alpha` block**

Find:

```hcl
    alpha = {
      replicaCount = var.dgraph_sec_alpha_replicas
      tolerations  = local.dgraph_sec_tolerations
```

Replace with:

```hcl
    alpha = {
      replicaCount = var.dgraph_sec_alpha_replicas
      logLevel     = var.dgraph_sec_alpha_log_level
      tolerations  = local.dgraph_sec_tolerations
```

- [ ] **Step 3: Wire `logLevel` into the `zero` block**

Find:

```hcl
    zero = {
      replicaCount = var.dgraph_sec_zero_replicas
      tolerations  = local.dgraph_sec_tolerations
```

Replace with:

```hcl
    zero = {
      replicaCount = var.dgraph_sec_zero_replicas
      logLevel     = var.dgraph_sec_zero_log_level
      tolerations  = local.dgraph_sec_tolerations
```

- [ ] **Step 4: Format and validate**

Run: `terraform fmt istari-k8s-core/ && (cd istari-k8s-core && terraform init -backend=false -input=false >/dev/null && terraform validate)`
Expected: `terraform fmt` reports the two files (alignment), `terraform validate` reports `Success! The configuration is valid.`

Run the helm-stack repo's pre-commit if present (the CLAUDE.md rule requires a blank line after `count`/`for_each`; these edits add neither, so no blank-line change is needed).

- [ ] **Step 5: Commit (do NOT push without user confirmation)**

```bash
gh auth switch --user mwelles-istari >/dev/null 2>&1 && \
git add istari-k8s-core/variables.tf istari-k8s-core/dgraph-sec.tf && \
git commit -m "feat(dgraph-sec): wire per-role log level through dgraph-sec.tf

Adds dgraph_sec_{alpha,zero}_log_level (default normal) consuming the
chart's new logLevel value. Ref: DGR-237"
```

---

## Task 8: Open the chart PR

**Files:** none

- [ ] **Step 1: Push the chart branch and open the PR** (confirm with user first)

```bash
gh auth switch --user mwelles-istari >/dev/null 2>&1 && \
git push -u origin DGR-237-dgraph-sec-logging-otel && \
gh pr create --repo istari-digital/charts --base main \
  --title "DGR-237: configurable log level + logging/OTEL docs for dgraph-sec" \
  --body "Implements DGR-237. Adds per-role logging values (logLevel, vmodule, logtostderr, alsologtostderr, logDir) wired to the Dgraph command line, and documents standard logging and OTEL tracing in the README. Terraform wiring lands on helm-stack PR #742. Design: docs/designs/2026-06-09-dgraph-sec-logging-otel-design.md"
```

- [ ] **Step 2: Link the work in Jira** (move DGR-237 to In Review, add the PR links)

---

## Self-Review

**Spec coverage:**
- Goal 1 (first-class per-role logging values) → Tasks 1–4.
- Goal 2 (document standard logging) → Task 5 `## Logging` + values-table rows.
- Goal 3 (document OTEL) → Task 5 `## Tracing (OpenTelemetry)`.
- Goal 4 (wire through dgraph-sec.tf, production-sane default) → Task 7.
- Edge cases (always-emit vs conditional, extraFlags override ordering, logtostderr footgun, integer passthrough) → covered by helper in Task 1 and render-asserts in Tasks 3–4; documented in Task 5.

**Placeholder scan:** No TBD/TODO; every code/template step shows full content; every command lists expected output.

**Type/name consistency:** `dgraph-sec.verbosity` and `dgraph-sec.logFlags` are defined in Task 1 and referenced by those exact names in Tasks 3–4. Value keys `logLevel`/`vmodule`/`logtostderr`/`alsologtostderr`/`logDir` match across values.yaml (Task 2), helper (Task 1), and README (Task 5). Terraform variables `dgraph_sec_alpha_log_level`/`dgraph_sec_zero_log_level` match between Steps 1–3 of Task 7.

**Open verification item for the executor:** confirm the `-sec` fork accepts `-v`, `--vmodule`, `--logtostderr`, `--alsologtostderr`, `--log_dir` via `dgraph-sec --help` (Task 6, Step 3 renders them; a smoke deploy confirms acceptance). These are standard glog flags, but the fork is the source of truth.
