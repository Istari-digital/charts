{{ if and .Values.zero.tls.enabled .Values.zero.tls.files }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ template "istari-dgraph-sec.zero.fullname" . }}-tls-secret
  namespace: {{ include "istari-dgraph-sec.namespace" . }}
  labels:
    {{- include "istari-dgraph-sec.labels" (dict "ctx" . "component" .Values.zero.name) | nindent 4 }}
  {{- with .Values.zero.tls.annotations }}
  annotations:
    {{- toYaml . | trimSuffix "\n" | nindent 4 }}
  {{- end }}
type: Opaque
data:
  {{- with .Values.zero.tls.files }}
    {{- toYaml . | trimSuffix "\n" | nindent 2 }}
  {{- end }}
{{ end }}
