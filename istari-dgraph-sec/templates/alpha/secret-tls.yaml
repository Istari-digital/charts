{{ if and .Values.alpha.tls.enabled .Values.alpha.tls.files }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ template "istari-dgraph-sec.alpha.fullname" . }}-tls-secret
  namespace: {{ include "istari-dgraph-sec.namespace" . }}
  labels:
    {{- include "istari-dgraph-sec.labels" (dict "ctx" . "component" .Values.alpha.name) | nindent 4 }}
  {{- with .Values.alpha.tls.annotations }}
  annotations:
    {{- toYaml . | trimSuffix "\n" | nindent 4 }}
  {{- end }}
type: Opaque
data:
  {{- with .Values.alpha.tls.files }}
    {{- toYaml . | trimSuffix "\n" | nindent 2 }}
  {{- end }}
{{ end }}
