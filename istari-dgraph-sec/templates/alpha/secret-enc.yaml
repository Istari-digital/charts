{{ if and .Values.alpha.encryption.enabled .Values.alpha.encryption.file }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ template "istari-dgraph-sec.alpha.fullname" . }}-encryption-secret
  namespace: {{ include "istari-dgraph-sec.namespace" . }}
  labels:
    {{- include "istari-dgraph-sec.labels" (dict "ctx" . "component" .Values.alpha.name) | nindent 4 }}
type: Opaque
data:
  {{- with .Values.alpha.encryption.file }}
    {{- toYaml . | trimSuffix "\n" | nindent 2 }}
  {{- end }}
{{ end }}
